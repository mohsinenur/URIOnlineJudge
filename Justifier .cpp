#include<iostream>
#include <sstream>
#include <string>
using namespace std;
main()
{
    int N;
    while(cin >> N && N != 0)
    {
        for(int i = 1; i <= N; i++)
        {
            string words[50];
            cin >> words;
        }
    }
}
